import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com" , "imgs.search.brave.com" , "source.unsplash.com"], // Allow Unsplash images
  },
};

export default nextConfig;
